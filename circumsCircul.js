// circumference of a circle

let radius = 7; // radius of the circle
let circumference = 2 * Math.PI * radius; // Circumference formula: C = 2 * π * r
console.log(
  `The circumference of the circle with radius ${radius} is ${circumference.toFixed(
    2
  )}`
); // Output the circumference rounded to 2 decimal places
// The circumference of the circle with radius 7 is 43.98
